# Lego data

The repository stores the small course subset used by the assignments. Images are organized as
NeRF-synthetic views with one manifest per split. `init.ply` is a starter point cloud, not a trained
answer.
