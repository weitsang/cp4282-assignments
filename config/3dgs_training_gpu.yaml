paths:
  data: ../data/lego
  output: ../outputs/warp-training.png
  init_ply: ../data/lego/init.ply

runtime:
  arch: cpu
  seed: 7

model:
  width: 256
  height: 256
  capacity: 100000
  initial_splats: 1000
  background: white

training:
  iterations: 2000

densification:
  interval: 100
  fraction: 0.2
